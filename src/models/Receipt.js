class Receipt {

  constructor(id, document_id, timestamp, name_from, name_to) {
    this.id = id;
    this.document_id = document_id;
    this.timestamp = timestamp;
    this.name_from = name_from;
    this.name_to = name_to;
  }

}

export default Receipt;
