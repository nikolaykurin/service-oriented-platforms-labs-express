class Invoice {

  constructor(id, document_id, timestamp, production, count) {
    this.id = id;
    this.document_id = document_id;
    this.timestamp = timestamp;
    this.production = production;
    this.count = count;
  }

}

export default Invoice;
